val $a_{0}$  = new Array[Long]($s_{0}$);
...
val $a_{l-1}$  = new Array[Long]($s_{l-1}$);

/* Начало циклической конструкции в модели PGAS */
for ($i$ in ${0,..,r - 1}$) {
    for ($j$ in $M$) {
        at ($j$) {
           $f_{0}(a_{0})$;
           ...
           $f_{l-1}(a_{l-1})$;
        }
   }
}
/* Конец конструкции */