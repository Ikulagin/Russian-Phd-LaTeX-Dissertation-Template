def BodyAt(a:Array[Long]) {
  ComputeSum(a);
}
...
public static def main (Rail[String]) {
  val a = [1,2,3];
  val world = Place.places();

  msg.code = Serialize(BodyAt);
  msg.data = Serialize(a);
  id = world.next(here);
  Send(id, msg);
}